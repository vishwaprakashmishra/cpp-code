#include <bits/stdc++.h>

using namespace std;
vector< int > graph[];

void make_chain(int n)
{
    for ( int i = 1; i < n ;i++ )
    {
        g[i].push_back(i+1);
    }
    g[i].push_back(1);
}
void make_branch(int no, int q){
    for( int i = n+1;i<=n+no ;i++ ){
        g[1].push_back(i);
    }
}
void MakeExtraChain(itn size, int no){
    while( no > 0){
        n++
    }
}
int main(void )
{
    int P , Q;
    cin>> P >> Q;
    
    return 0;
    
}
