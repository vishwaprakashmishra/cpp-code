#include<bits/stdc++.h>
using namespace std;
void ad(vector<int> &v1 ,vector<int> &v2){
    int borrow, res, n,m ;
    n = v1.size();
    m = v1.size();
    if( n < v2.size()){
        n = v2.size();
        m = v1.size();
    }
    for( int i = n-1, j = m-1 ; i >= 0 ; i--){
        borrow = v1
    }
}